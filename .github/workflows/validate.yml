name: Validate Windows scripts

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run static validation
        shell: pwsh
        run: ./tools/validate-scripts.ps1
